import 'package:flutter/material.dart';

class ColorConstants {
  static Color primaryColor = Color(0xff2563EB);
  static Color secondaryColor = Color(0xffabd2e0);
  static Color canvasColor = Color(0xFF2E2E48);
  static Color textFieldHintColor = Color(0x8023233C);
  static Color successStateColor = Color(0xff10B981);
  static Color pendingStateColor = Color(0xffF59E0B);
  static Color backgroundColor = Colors.white;
  static Color cardBackgroundColor = Color(0xffF3F4F6);
  static Color textColorPrimary = Color(0xff1F2937);
  static Color textColorSecondary = Color(0xff6B7280);
  static Color failureColor = Color(0xfff58282);
  static Color transparent = Colors.transparent;
  static Color danger = Colors.red;


  // static Color failureColorWithOpacity = failureColor.withValues(alpha: 0.5);

  static const primaryDark = Color(0xFF0A0B14);   // (nearly black background)
  static const dark = Color(0xFF12131E);   // (card backgrounds)
  static const tertiaryDark = Color(0xFF1A1C2D);   // (elevated elements)

  // Neon Accent Colors:
  static const blue = Color(0xFF3C7EFF);   // (main accent color)
  static const electricPurple = Color(0xFF9945FF);   // (secondary accent)
  static const neonPink = Color(0xFFFF3D87);   // (highlight elements)
  static const cyanTeal = Color(0xFF00E1D9);   // (tertiary accent)

  // Gradient Combinations:
  static const bluePurple = Color(0xFF3C7EFF);   // (buttons, highlights)
  static const pinkPurple = Color(0xFFFF3D87);   // (accent elements)
  static const cyanBlue = Color(0xFF00E1D9);   // (data visualizations)

  // Status Colors:
  static const successGreen = Color(0xFF00E165);   // (completed tasks)
  static const warningOrange = Color(0xFFFF9D00);   // (urgent pause)
  static const alertRed = Color(0xFFFF3D5A);   // (urgent skip, high priority)
  static const infoBlue = Color(0xFF3C7EFF);   // (medium priority)
  static const neutralPurple = Color(0xFF9945FF);   // (low priority)

  // Text Colors:
  static const primaryWhite = Color(0xFFFFFFFF);   // (main text)
  static const secondaryWhite = Color(0xFFD8D9E1);   // (secondary text)
  static const tertiaryGray = Color(0xFF8A8D9F);
}